# tax-receipts-organizer
This is a Unity 4 application that will, given a file containing ' ' delimited items (name, month, day, amount), and sort them with the following precedence: month-day-amount).
